Algoritmo "problemaSoma"

Var
   x, y, soma : inteiro

Inicio
      escreval("Digite o valor para X:")
      leia(x)
      escreval("Digite o valor para Y:")
      leia(y)
      
      soma <- x + y
      
      escreval("Soma = ", soma)

Fimalgoritmo